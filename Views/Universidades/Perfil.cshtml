@model Inluirme_Proyecto.Models.Universidades

@{
    ViewBag.Title = "Perfil";
    Layout = null;
}
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
<div class="container text-right">
    @{
        if (User.Identity.IsAuthenticated)
        {
            <a class="nav-link text-danger" href="/Inicio/Logout">Cerrar sesion</a>
        }
    }

</div>

<h2>DATOS PERSONALES</h2>
<hr />

<div class="card col-5">
    <h5 class="card-header">@Model.nombre</h5>
    <div class="card-body">
        <p>C.U.I.T.: @Model.cuit</p>
        <p>Provincia: @Model.provincia</p>
        <p>Ciudad: @Model.ciudad</p>
        <p>Domicilio: @Model.domicilio</p>
        <p>Telefono: @Model.telefono</p>
        <p>Correo Electronico: @Model.mail</p>

        <div class="row">
            <a href="@Url.Action("Capacitaciones","CapacitarUniversidad",new { id = Model.id })" class="nav-link">Ver Capacitaciones</a>
            <a href="@Url.Action("VerBecas","Becas",new { id = Model.id })" class="nav-link">Ver Becas</a>
            <a href="@Url.Action("Editar","Universidades",new { id = Model.id })" class="nav-link text-warning">Editar perfil</a>
            <a href="@Url.Action("Eliminar","Universidades",new { id = Model.id})" onclick="return eliminar()" class="nav-link text-danger">Eliminar perfil</a>
        </div>

    </div>
</div>

<script>
    function eliminar() {
        if (confirm("¿Desea eliminar la universidad de nuestra pagina?"))
            return true
        else
            return false
    }
</script>